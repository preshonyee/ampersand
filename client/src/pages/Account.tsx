import styled from "styled-components";

const Wrapper = styled.div``;

const AccountPage = () => {
  return (
    <Wrapper>
      <h1>Account</h1>
    </Wrapper>
  );
};

export default AccountPage;
